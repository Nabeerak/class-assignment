// a program that checks if a given number is positive, negative, or zero.
let no: number = -5;
if (no > 0) {
  console.log("this is a positive number");
} else if (no < 0) {
  console.log("this is a negative number");
} else if (no == 0) {
  console.log("zero");
}
